{
  "name": "nutrition-research-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "deploy:convex": "cd packages/backend && npx convex deploy",
    "convex:dev": "cd packages/backend && npx convex dev"
  },
  "devDependencies": {
    "turbo": "^2.8.1",
    "typescript": "5.9.3"
  }
}
